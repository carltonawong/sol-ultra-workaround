# Merge these keys into ~/.codex/config.toml.
# If this table already exists, do not create it twice.
[features.multi_agent_v2]
enabled = true
hide_spawn_agent_metadata = false
max_concurrent_threads_per_session = 4
